/// Formal lineage metadata for the Operation System, independent from the
/// application, project, and release revision versions.
abstract final class OperationSystemMetadata {
  static const version = 'AX 0.9.0';
}
