import 'package:flutter/material.dart';

class MorningInputForm extends StatelessWidget {
  const MorningInputForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
